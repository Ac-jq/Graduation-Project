# 《全栈核心技术学习与面试防守宝典》

> 项目名称：**基于大语言模型的高校心理健康辅导与协同管理平台**  
> 技术主线：**Vue 3 + TypeScript + Pinia + Spring Boot 3 + MyBatis-Plus + MySQL + Sa-Token + Redis + WebSocket + Spring AI + DeepSeek**  
> 阅读目标：**不是背概念，而是把这个项目拆成可以在答辩、实习面试、秋招技术面里直接输出的“防守答案”。**

---

## 一、先给自己一个总定位

如果面试官只给你 30 秒介绍项目，你可以先这样说：

> 我做的是一个面向高校场景的智能心理健康平台，核心不是单纯接一个大模型聊天，而是把学生自助倾诉、测评报告、资源推荐、预约咨询、真人聊天室，以及后台用户治理和 AI 运维串成一个闭环。  
> 技术上我重点做了三件事：  
> 第一，用 Spring AI 和 DeepSeek 做了大模型能力接入，但对高风险操作做了降维保底；  
> 第二，在后端把鉴权、密码安全、消息加密、关系型权限校验和审计日志做成了一套完整链路；  
> 第三，在前端解决了复杂界面的状态同步、嵌套滚动、直连 CRUD 交互和聊天体验问题，让系统既能演示，也能稳定运行。

---

## 二、怎么使用这份宝典

这份文档按 4 个模块拆开：

1. **AI 与大模型工程化模块**
2. **后端、数据库与安全防护模块**
3. **前端交互、UI/UX 与网络请求模块**
4. **业务闭环与架构思维**

每个技术点都固定按 4 个维度展开：

1. **一句话本质（What）**
2. **为什么用它（Why）**
3. **底层工作原理（How）**
4. **大厂面试连环拷问（Q&A）**

你在答辩或面试时，不需要把每一段完整背下来。正确用法是：

- **先记住 What 和 Why**
- **再用 How 讲出 3 到 4 步的真实执行链路**
- **最后用 Q&A 扛住追问**

---

# 模块一：AI 与大模型工程化模块

## 1. Spring AI + DeepSeek 兼容接入

### 1. 一句话本质（What）
Spring AI 本质上就是一个帮你把“大模型调用”封装成 Spring 风格服务层的中间层，让模型调用、工具调用、配置管理都更像普通 Java 业务代码。

### 2. 为什么用它（Why）
这个项目不只是要“调一次接口”，而是要同时支撑：

- 学生 AI 倾诉对话
- 测评报告 AI 解读
- 推荐资源筛选
- 管理后台 AI 运维解析

如果全部手写 HTTP 请求，后期会散、乱、难控。Spring AI 的价值在于把模型调用统一到服务层，让 AI 变成和数据库、Mapper 一样的基础能力。

### 3. 底层工作原理（How）
1. 在 `src/main/resources/application.yml` 中配置 DeepSeek 的兼容 OpenAI 地址、模型名、密钥和超时参数。  
2. 在 `src/main/java/sdu/jiaq/jqpro/service/ai/impl/AdminOpsAiClientImpl.java` 中通过 `OpenAiChatModel` 发起请求。  
3. 管理端解析使用 `OpenAiChatOptions.builder()`，明确指定模型、温度、最大 token，以及 tool callbacks。  
4. 服务层收到结果后，不直接相信模型自然语言，而是先看有没有 tool call，再进入后端确定性执行。

### 4. 大厂面试连环拷问（Q&A）
**Q1：你为什么不用最原始的 RestTemplate 或 OkHttp 直接调 DeepSeek？**  
A：可以，但那样 AI 能力会散落在多个 service 里。Spring AI 的优势是把模型调用抽象成统一组件，后期如果要换模型、加 tools、加系统提示词、做多种 AI 能力隔离，维护成本更低。

**Q2：Spring AI 是不是只是“好看一点”的封装？**  
A：不是。它的价值不在“少写几行代码”，而在于把模型交互标准化。尤其这个项目同时有聊天、解读、运维三种 AI 能力，如果没有统一封装，prompt、超时、鉴权、日志和工具注册都会很快失控。

---

## 2. Prompt 约束不是“文案优化”，而是业务协议

### 1. 一句话本质（What）
提示词工程的本质不是“把模型哄开心”，而是把模型纳入一个可执行的业务协议。

### 2. 为什么用它（Why）
后台 AI 运维最怕两件事：

- 模型乱说“已经成功删除”，但其实数据库根本没动
- 缺参数时模型瞎猜，导致误删、误改

所以我把 prompt 从“开放聊天”改成了“强约束指令协议”，让模型在缺参数时只返回固定中文提示，而不是硬调工具。

### 3. 底层工作原理（How）
1. 在 `application.yml` 的 `jqpro.ai.admin-ops.system-prompt` 中，明确规定模型只处理当前输入，不做自由发挥。  
2. Prompt 明确告诉模型：只有 4 个工具可用，且参数必须平铺。  
3. Prompt 还规定：如果新增、删除、修改缺参数，必须返回 `【缺少参数，请补充：xxx】`，不要调用工具。  
4. 后端在 `AdminOpsAiClientImpl.java` 中读取该 prompt，作为管理员运维解析的系统指令。

### 4. 大厂面试连环拷问（Q&A）
**Q1：为什么你要把“缺参数时的回复格式”写死？**  
A：因为后台运维不是开放问答，而是流程控制。固定输出格式能让前端准确识别“这是缺参提示”，从而保留输入框内容、弹窗提醒、避免误执行。

**Q2：如果 prompt 写得太死，会不会损失模型灵活性？**  
A：会损失一部分自由表达，但这是我刻意做的权衡。面对学生倾诉，我更重视自然语言；面对后台删改查，我更重视可控性和可追责性。

---

## 3. Function Calling 的扁平化设计

### 1. 一句话本质（What）
Function Calling 的关键不是“会不会调函数”，而是“函数参数结构是不是足够让模型稳定提取”。

### 2. 为什么用它（Why）
这个项目一开始踩过一个典型坑：把 `target_filters`、`update_fields` 设计成嵌套 JSON，结果模型经常提不准，后端又接收不到，最后出现“数据库里有人，但系统说找不到目标”。

所以后来我把参数降维成扁平结构，降低模型理解成本。

这里我后面又做过一次优化：

- **之前怎么做**：后端既让模型产出工具参数，又在服务层继续从原始自然语言里二次猜字段，甚至补充目标条件和新值。这样虽然看起来“兜底很多”，但很容易把模型已经提对的参数再次污染。  
- **现在怎么优化**：把职责重新收口成“模型负责识别操作类型、选择工具、填充扁平参数；后端只做必要校验和执行”。尤其是 `create_user`、`update_users`、`delete_users` 这几类写操作，不再让后端反复从原句二次猜测，减少了误命中和多人误改。

### 3. 底层工作原理（How）
1. 在 `AdminOpsAiClientImpl.java` 中注册平铺工具，例如 `query_users`、`create_user`、`update_users`、`delete_users`。  
2. 参数不再嵌套，而是直接给如 `account`、`realName`、`studentNo`、`college`、`grade`、`newCollege`、`newGrade` 这样的平铺字段。  
3. 我又把 `role_code` 这种模型容易漏掉但业务能明显推断出的字段写进 tool schema 说明里，例如“原句出现学生/学号，就必须自动填 `STUDENT`”。  
4. 模型只负责抽取意图和字段，不负责直接操作数据库。  
5. 服务层在 `AdminAiTaskServiceImpl.java` 中把 tool call 转成后端的过滤条件或修改命令。

### 4. 大厂面试连环拷问（Q&A）
**Q1：为什么嵌套 JSON 比平铺字段更容易出错？**  
A：因为模型在自然语言里本来就要做一次语义映射，如果工具参数再要求它同时构造多层对象，出错概率会明显升高。尤其中文自然表达多变，平铺字段更适合作为“最小稳定面”。

**Q2：那扁平化是不是设计退步了？**  
A：不是退步，是工程折中。学术上嵌套结构更优雅，但项目目标是稳定演示和稳定 CRUD。对于高频简单操作，平铺字段比抽象优雅更重要。

---

## 4. 从“多轮上下文 Agent”降维到“单次无状态解析”

### 1. 一句话本质（What）
单次无状态解析的本质，是把复杂 Agent 的“会话记忆”能力换成更稳定、更可控的“一次输入一次判定”。

### 2. 为什么用它（Why）
这个项目的后台 AI 运维一开始尝试过多轮上下文模式，但很快暴露出两个问题：

- 上下文容易串、容易失忆
- 删除和修改这种高风险动作，一旦依赖自由对话，就很难保证正确率

所以最后我做了工程上的降维：**简单 CRUD 优先确定性执行，模型只做单次指令解析。**

这个重构不是一次性完成的，而是分两步走：

- **第一阶段（早期方案）**：我尝试过“保留历史对话 + 多轮补参 + 本地规则兜底”的 Agent 方案。优点是看起来更像智能体，缺点是上下文会串，规则兜底还会覆盖模型结果。  
- **第二阶段（最终方案）**：我改成“单次无状态解析 + 写操作纠偏提示 + 前端直连 CRUD”。管理员每次尽量一次性把话说完整；模型只解析这一句；如果模型把修改误判成查询，后端只做一次轻量纠偏提示，不再把整个流程拉回复杂上下文。

### 3. 底层工作原理（How）
1. `AdminOpsAiClientImpl.java` 现在的主要思路是：每次只把当前管理员输入送给模型。  
2. 不再把上一轮历史消息强制拼进去，让模型避免被“旧上下文”污染。  
3. 如果模型把明显的新增、删除、修改误判成 `query_users`，后端会再发一次“纠偏提示”，要求它改用正确工具，而不是自己在本地用复杂规则强行改写。  
4. 如果模型返回缺参提示，前端只是保留输入内容让管理员补全，但下一次仍然是“完整输入重新解析”。  
5. 真正的删除、修改执行，仍由后端 REST 接口和数据库事务负责。

### 4. 大厂面试连环拷问（Q&A）
**Q1：你为什么放弃多轮 Agent？是不是做不出来？**  
A：不是做不出来，而是经过项目验证后，我认为它不适合作为答辩和后台运维的主流程。学生聊天适合多轮，后台删改查更适合单次确定性。这个决定体现的是工程判断，不是能力退让。

**Q2：什么时候你会重新引入多轮上下文？**  
A：当任务是“辅助分析”而不是“高风险执行”时，例如复杂报表解释、资源推荐理由生成、学生情绪趋势分析，这类可以重新启用上下文，因为容错空间更大。

---

## 5. “模型负责理解，后端负责执行”的双层架构

### 1. 一句话本质（What）
大模型在这个项目里不是“最终裁决者”，它只是一个语义理解器；真正的业务执行权在后端服务层。

### 2. 为什么用它（Why）
如果把“理解意图”和“落库执行”都交给模型，一旦模型误解语句，就可能产生不可逆的后果，尤其是批量删除、修改用户信息这种操作。

这里也经历过一次架构收缩：

- **之前怎么做**：模型先给出结果，后端再叠加大段规则兜底、字段补全、名字模糊纠偏、原句二次推断。这样虽然看起来更保险，但实际上把系统变成了“双解释器”，很难判断最终是谁说了算。  
- **现在怎么优化**：我把决策权前移给模型，把执行权留给后端。也就是说，模型负责“这是什么操作、该选哪个工具、参数是什么”；后端负责“这些参数能不能执行、影响哪些数据、什么时候真正落库”。

### 3. 底层工作原理（How）
1. 模型在 `AdminOpsAiClientImpl.java` 中先产出 tool call 或缺参提示。  
2. 如果是写操作但模型误用了查询工具，后端只补一层非常轻的纠偏提示，让模型重新选择工具，不再自己重写参数。  
3. `AdminAiTaskServiceImpl.java` 会把 tool call 转成后端内部过滤条件。  
4. 对于删除和修改，不是直接执行，而是先生成任务项 `AdminAiTaskItem` 作为预览数据。  
5. 只有前端勾选并确认后，才会调用真正的用户 CRUD 接口落库。

### 4. 大厂面试连环拷问（Q&A）
**Q1：为什么不让模型直接决定“删哪个人”？**  
A：因为模型适合理解语言，不适合拥有最终执行权。数据库修改必须经过确定性校验、预览确认和事务提交，这些责任应该留在后端。

**Q2：这是不是让 AI 变弱了？**  
A：从“幻想式 Agent”看是变弱了，但从企业可用性看反而更强了。企业后台最怕的不是 AI 不聪明，而是 AI 自作主张。

---

## 6. 聊天场景下的上下文窗口控制

### 1. 一句话本质（What）
上下文窗口控制的本质，就是在“保留必要记忆”和“避免 prompt 爆炸”之间做平衡。

### 2. 为什么用它（Why）
学生 AI 聊天和后台 CRUD 不一样。学生聊天需要上下文，但也不能无限把所有历史消息都塞给模型，否则成本高、延迟大、还容易让模型抓不住重点。

### 3. 底层工作原理（How）
1. 在 `AiChatServiceImpl.java` 中定义 `MAX_HISTORY_MESSAGES = 10`。  
2. 每次学生发消息时，先从数据库读取最近 10 条消息。  
3. 再按时间顺序整理成 `AiChatAiRequest.ConversationMessage` 列表。  
4. 最终把“最近上下文 + 当前用户输入”发给 `AiChatAiClient.generateReply(...)`。

### 4. 大厂面试连环拷问（Q&A）
**Q1：为什么是 10 条，不是 20 条或者全部？**  
A：10 条不是理论最优值，而是当前项目的工程折中。它能保留最近情绪语境，又不会让请求体太大。这个值可以继续按真实用户时长和成本曲线再调优。

**Q2：如果学生在 30 轮之前提到过关键信息怎么办？**  
A：当前版本会有遗忘风险，所以我把真实会话全部落库，后续可以做摘要记忆，把“早期关键信息”压缩成结构化摘要，再参与后续 prompt。

---

## 7. AI 风险词预判不是“锦上添花”，而是安全阀

### 1. 一句话本质（What）
风险词预判就是在模型回答之前，先用一层确定性的安全规则对高危内容做快速拦截和标记。

### 2. 为什么用它（Why）
心理健康场景和普通聊天系统不同。学生如果提到自伤、自杀、结束生命等内容，系统不能把全部风险都寄托给模型临场发挥。

### 3. 底层工作原理（How）
1. `AiChatServiceImpl.java` 中定义了 `HIGH_RISK_KEYWORDS`。  
2. 学生发消息后，先执行 `analyzeRisk(content)`，识别是否命中高危词。  
3. 命中后会把本轮消息风险等级标记为高，并带上关键词。  
4. 这些风险信息会一起参与 AI 回复生成，并写回会话状态，便于后续展示、归档和干预。

### 4. 大厂面试连环拷问（Q&A）
**Q1：为什么不用模型自己判断风险？**  
A：模型可以辅助判断，但不能作为唯一安全机制。关键词预判是低成本、高确定性的第一道阀门，尤其适合拦截极端风险词。

**Q2：关键词预判会不会误伤正常表达？**  
A：会有误报可能，所以我把它设计成“风险标记”而不是“直接封禁”。它的作用是提高系统敏感度，不是替代人工判断。

---

## 8. AI 导师形象设置为什么要按学生隔离

### 1. 一句话本质（What）
AI 导师设定按学生隔离，本质上是在做“多租户思维”的最小化落地：每个学生只看到自己的 AI 形象和昵称。

### 2. 为什么用它（Why）
如果 AI 导师头像和昵称是全局共享的，就会出现 A 学生改了设置，B 学生也跟着变的串数据问题，这在隐私和体验上都不能接受。

### 3. 底层工作原理（How）
1. `AiPersonaSetting` 表中按 `studentUserId` 存储独立设定。  
2. `AiPersonaSettingServiceImpl.java` 通过 `SecurityUtil.getCurrentUserId()` 获取当前学生。  
3. 查询和更新都严格按当前登录学生的 `studentUserId` 执行。  
4. 查不到时返回默认 AI 设定，查得到时返回该学生自己的配置。

### 4. 大厂面试连环拷问（Q&A）
**Q1：这是不是过度设计？直接存前端 localStorage 不就行了？**  
A：如果只是 Demo，本地存储够用；但一旦涉及跨设备同步、账号隔离、老师端查看会话风格一致性，就必须落到后端并按用户隔离。

**Q2：这种设计和多租户系统有什么共性？**  
A：共性就在于“所有数据查询都必须带租户边界”。这里的租户不是企业，而是学生用户本身。核心思想是一致的。

---

# 模块二：后端、数据库与安全防护模块

## 9. 统一 Result 返回和统一异常体系

### 1. 一句话本质（What）
统一返回体和统一异常体系，本质上是在给前后端建立一套可预测、可编排、可审计的通信协议。

### 2. 为什么用它（Why）
这个项目接口很多，角色也多。如果每个 Controller 各自返回不同结构，前端会很痛苦；如果每个业务都乱抛异常，日志和用户提示也会很乱。

### 3. 底层工作原理（How）
1. 所有接口统一返回 `Result<T>`，代码在 `src/main/java/sdu/jiaq/jqpro/common/result/Result.java`。  
2. 所有业务错误统一抛 `BusinessException`，代码在 `common/exception/BusinessException.java`。  
3. `GlobalExceptionHandler.java` 负责把业务异常、参数校验异常、登录异常、权限异常全部转成标准 `Result`。  
4. 前端 Axios 只需要识别统一的 `code / message / data` 结构。

### 4. 大厂面试连环拷问（Q&A）
**Q1：为什么既要 HTTP 状态码，又要业务 code？**  
A：HTTP 状态码更多面向协议层和基础设施，比如网关、浏览器、监控；业务 code 面向前端和业务协同。两者不是重复，而是不同层级的表达。

**Q2：为什么不直接在 Controller 里 try-catch？**  
A：那样会让错误处理分散且容易漏。统一异常处理器更符合分层职责，也方便后续接入统一日志和审计策略。

---

## 10. Sa-Token 鉴权不是“会登录就行”，而是完整的会话治理

### 1. 一句话本质（What）
Sa-Token 本质上是在帮项目建立“谁登录了、能看什么、能干什么、多久失效”的会话治理体系。

### 2. 为什么用它（Why）
这个系统至少有学生、咨询师、管理员三类角色，而且很多数据不是“登录就能看”，而是“登录 + 角色 + 关系”才能看，所以必须有明确的鉴权框架。

### 3. 底层工作原理（How）
1. `SaTokenConfig.java` 通过 `SaInterceptor` 保护 `/api/**` 路径。  
2. `/api/auth/login`、`/api/auth/register` 等少量接口被加入排除名单。  
3. 登录成功后，`AuthServiceImpl.java` 调用 `StpUtil.login(userId)`，Sa-Token 生成令牌。  
4. `SaTokenPermissionProvider.java` 通过数据库查询当前用户 `roleCode`，把角色映射给框架使用。

### 4. 大厂面试连环拷问（Q&A）
**Q1：为什么选 Sa-Token 而不是 Spring Security？**  
A：Spring Security 更重、更全，但对这个项目的学习和迭代成本更高。Sa-Token 对单体后台、RBAC、前后端分离、快速会话管理更轻量，比较适合当前场景。

**Q2：Sa-Token 的角色校验够用吗？**  
A：角色校验只解决“这个人是不是学生/老师/管理员”。真正细粒度的数据权限，我是在业务层再补了一层关系校验，比如老师必须和学生存在绑定或预约关系。

---

## 11. 密码为什么要做“加盐哈希”，而不是直接 MD5

### 1. 一句话本质（What）
加盐哈希的本质，就是让相同密码在数据库里长得不一样，从而提升撞库和字典攻击成本。

### 2. 为什么用它（Why）
我本身是网络与信息安全方向，所以在这个项目里，我不接受明文存储，也不接受直接无盐 MD5。学生和老师账号都属于真实身份体系，必须做基本密码安全。

### 3. 底层工作原理（How）
1. `PasswordCryptoUtil.java` 使用 `SecureRandom` 生成 16 字节随机盐。  
2. 再使用 Bouncy Castle 提供的 `SHA3-256` 对 `salt + ":" + rawPassword` 做哈希。  
3. 最终把 `passwordSalt` 和 `passwordHash` 分开存到 `sys_user` 表。  
4. 登录时通过 `PasswordCryptoUtil.matches(raw, salt, hash)` 重新计算并比对。

### 4. 大厂面试连环拷问（Q&A）
**Q1：为什么选 SHA3-256，而不是 BCrypt？**  
A：如果从口令哈希专业性看，BCrypt、PBKDF2、Argon2 更适合密码场景。这个项目当前使用 SHA3-256 + 随机盐，已经比明文或无盐散列强很多，但如果继续走生产级，我会优先升级到专用口令哈希算法。

**Q2：盐为什么要分开存？分开存是不是没意义？**  
A：盐不需要保密，但需要和哈希一起保存以便登录校验。它的作用不是“藏起来”，而是打散相同密码的相同哈希值。

---

## 12. 聊天消息为什么要做落库加密

### 1. 一句话本质（What）
落库加密的本质，就是即使数据库被直接查看，也不能立刻看到学生和咨询聊天的明文内容。

### 2. 为什么用它（Why）
心理咨询和 AI 倾诉内容都属于高敏感数据。这个项目不只是做功能演示，还要体现出信息安全意识，所以我对聊天内容做了存储层加密。

### 3. 底层工作原理（How）
1. `ChatCryptoUtil.java` 内部用 Hutool `SecureUtil.aes(KEY)` 构造 AES 工具。  
2. KEY 来自固定秘密经过 SHA-256 后截取前 16 字节。  
3. 写消息时调用 `encryptWithPrefix("enc::")`，明文会变成带前缀的密文。  
4. 读消息时调用 `decryptCompat(...)`，兼容旧数据和新密文格式。

### 4. 大厂面试连环拷问（Q&A）
**Q1：为什么要加 `enc::` 前缀？**  
A：这是兼容性设计。系统演进过程中可能有老数据是明文，前缀能帮助我快速判断当前字段是否需要解密，避免读历史数据时报错。

**Q2：这个密钥写在代码里安全吗？**  
A：严格生产环境不安全，应该放 KMS、环境变量或配置中心。当前项目为了本地答辩稳定性做了硬编码默认值，这是教学演示和工程安全之间的阶段性折中，我自己也会明确指出这一点。

---

## 13. MyBatis 动态 SQL 的本质是“条件拼接”，也是 Bug 高发区

### 1. 一句话本质（What）
动态 SQL 不是“写 SQL 的高级版”，而是在运行时根据参数决定 WHERE 条件的拼接方式。

### 2. 为什么用它（Why）
后台用户列表和 AI 运维查询都需要支持多条件过滤，比如：

- 账号
- 真实姓名
- 显示名
- 学号
- 学院
- 年级

如果每个组合都写一个 SQL，成本太高。

### 3. 底层工作原理（How）
1. `SysUserMapper.java` 中的 `selectAdminUserSummaries(...)` 用注解 SQL 拼接动态查询。  
2. 只有参数不为空时，才加上对应的 WHERE 子句。  
3. 模糊查询统一用 `LOWER(...) LIKE CONCAT('%', LOWER(#{keyword}), '%')` 处理，规避大小写差异。  
4. 服务层 `AdminUserServiceImpl.java` 和 `AdminAiTaskServiceImpl.java` 会先做字符串归一化，再调用 Mapper。

### 4. 大厂面试连环拷问（Q&A）
**Q1：动态 SQL 最容易踩什么坑？**  
A：第一是 `AND / OR` 优先级错，查账号结果变成全表；第二是 `null` 和空字符串没处理好；第三是模糊查询字段选错，导致“查学号没结果，查账号返回全部人”。

**Q2：为什么还要在 Java 层再做一次过滤？**  
A：因为自然语言抽参并不总是稳定，服务层二次过滤是为了给查询结果再上一层保险，降低误命中概率。

---

## 14. `sys_user + student_profile` 的 LEFT JOIN 是后台查询的关键

### 1. 一句话本质（What）
LEFT JOIN 的本质，是把用户基础身份表和学生扩展档案表拼成一个后台可查询的统一视图。

### 2. 为什么用它（Why）
学生的账号、姓名、角色在 `sys_user`，但学院、年级、性别在 `student_profile`。如果不联表，就没法支持“查询人工智能学院 2026 级学生”这种业务。

### 3. 底层工作原理（How）
1. `SysUserMapper.java` 中使用 `FROM sys_user u LEFT JOIN student_profile sp ON u.id = sp.user_id`。  
2. 账号、姓名、显示名、学号走 `u` 表字段。  
3. 学院、年级走 `sp.college`、`sp.grade`。  
4. LEFT JOIN 而不是 INNER JOIN，是为了兼容非学生角色或档案不完整的情况。

### 4. 大厂面试连环拷问（Q&A）
**Q1：为什么用 LEFT JOIN，不用 INNER JOIN？**  
A：因为后台用户列表不只有学生，还有咨询师和管理员。如果用 INNER JOIN，就会把没有学生档案的角色直接过滤掉。

**Q2：LEFT JOIN 会不会导致查询结果过大？**  
A：会有一定成本，所以我把它主要用于后台治理查询，不会在高频聊天链路里用。后续如果数据量再上去，可以按角色拆接口或加组合索引。

---

## 15. RESTful 设计不是教条，而是让前端更稳定

### 1. 一句话本质（What）
RESTful 的本质，是让“资源是什么、怎么改、谁来改”在接口层一眼就能看明白。

### 2. 为什么用它（Why）
管理员 AI 运维最后能稳定落地，靠的不是模型，而是清晰的 CRUD 接口：

- `POST /api/admin/users`
- `PUT /api/admin/users/{id}`
- `DELETE /api/admin/users/{ids}`

这让前端即使绕过复杂 AI，也能直连完成核心操作。

### 3. 底层工作原理（How）
1. `AdminUserController.java` 提供管理员用户管理接口，并加 `@SaCheckRole(RoleConstants.ADMIN)`。  
2. `AdminUserServiceImpl.java` 承接真正的创建、更新、启停用、删除逻辑。  
3. 删除接口支持逗号分隔 ID，服务层再逐个删除，保证可兼容单删和批删。  
4. 管理端 AI 只负责解析出要操作的用户，真正写库仍然走这些标准接口。

### 4. 大厂面试连环拷问（Q&A）
**Q1：为什么不让 AI 服务自己直接改数据库？**  
A：因为 CRUD 接口本身就是系统边界。AI 只是输入层，不能绕开已存在的权限、审计、校验和事务体系。

**Q2：批量删除用路径参数是不是不够优雅？**  
A：如果做更标准的企业接口，我会改成 `DELETE /api/admin/users` + request body 传 ID 列表。但当前这个实现更方便前端直连和快速验证，属于工程上的实用取舍。

---

## 16. 关系型权限比角色权限更难，也是这个项目的真正门槛

### 1. 一句话本质（What）
角色权限回答的是“你是谁”，关系型权限回答的是“你和这条数据是什么关系”。

### 2. 为什么用它（Why）
老师不是只要有“咨询师”角色，就能看任意学生的 AI 会话和报告。必须满足：

- 已绑定辅导关系  
或
- 真实发生过已接受/已完成的预约关系

这才符合心理数据最小可见原则。

### 3. 底层工作原理（How）
1. `AiChatServiceImpl.java` 中 `verifyCounselorStudentOwnership(...)` 先查 `counselor_student`。  
2. 如果没绑定，再查 `consult_appointment` 是否存在 `ACCEPTED / COMPLETED` 的合法预约关系。  
3. 两者都没有，就抛 `BusinessException("无权查看该学生AI会话")`。  
4. 这种思路同样用于报告、真人聊天等敏感数据接口。

### 4. 大厂面试连环拷问（Q&A）
**Q1：为什么不能只靠角色？**  
A：因为角色太粗。咨询师角色只能说明“你是老师”，不能说明“你有权看这个学生”。真实业务里，数据权限通常都要叠加数据关系判断。

**Q2：这种关系权限怎么防止性能差？**  
A：当前是单体项目，关系判断次数可控。如果规模继续扩大，我会把“老师可见学生集合”做缓存或预计算索引，避免每次都查多张表。

---

## 17. 审计日志的价值不只是“留痕”，更是安全责任链

### 1. 一句话本质（What）
审计日志就是给关键操作留下“谁、在什么时候、对什么对象、做了什么”的证据链。

### 2. 为什么用它（Why）
这个项目里涉及：

- 登录
- 注册
- 改密码
- 后台增删改用户
- 启用/禁用账号

这些都需要追责能力，尤其在心理健康和教育管理场景下，日志是必需品。

### 3. 底层工作原理（How）
1. `AuthServiceImpl.java` 在登录、注册、修改密码时调用 `auditLogService.record(...)`。  
2. `AdminUserServiceImpl.java` 在创建用户、更新资料、重置密码、启停用、删除用户时同样记录日志。  
3. 日志写入 `sys_audit_log` 对应的 Mapper 和实体。  
4. 后台再通过审计日志页面进行查询和回溯。

### 4. 大厂面试连环拷问（Q&A）
**Q1：审计日志和普通业务日志有什么区别？**  
A：普通业务日志偏向排查程序问题，审计日志偏向追踪用户行为和安全责任。前者服务研发，后者服务治理和合规。

**Q2：如果审计日志写失败怎么办？会影响主流程吗？**  
A：当前实现里审计和主流程耦合较紧，优点是留痕更完整，缺点是理论上会影响部分主链路。生产级可以考虑异步化，但要平衡一致性和吞吐。

---

## 18. WebSocket 不是“连上就行”，关键是连接前校验和连接后广播

### 1. 一句话本质（What）
WebSocket 的本质，是在一次握手成功后，建立一个可持续收发消息的全双工通道。

### 2. 为什么用它（Why）
真人咨询聊天室必须支持近实时消息推送。如果每发一条消息都走轮询，延迟高、体验差、状态同步也麻烦。

### 3. 底层工作原理（How）
1. `WebSocketConfig.java` 注册 `/ws/consult-chat` 入口，并挂载握手拦截器。  
2. `ConsultChatHandshakeInterceptor` 在握手阶段把 `appointmentId`、`userId` 等信息塞进会话属性。  
3. `ConsultChatWebSocketHandler.java` 在 `afterConnectionEstablished` 里校验身份和房间合法性。  
4. 收到消息后，调用 `ConsultChatServiceImpl.sendMessage(...)` 落库，再向房间内双方广播 `MESSAGE`、`USER_JOINED`、`CHAT_CLOSED` 等事件。

### 4. 大厂面试连环拷问（Q&A）
**Q1：为什么要在握手和消息处理两个阶段都做校验？**  
A：握手校验解决“能不能进房间”，消息处理校验解决“进来以后还能不能继续发、会话是否已结束”。两层都要有。

**Q2：如果一方断线了怎么办？**  
A：当前 `ConsultChatWebSocketHandler` 会清理连接并广播 `USER_LEFT`。这个机制至少保证房间状态和 UI 能感知对方离线。

---

## 19. Redis 在这个项目里的真实角色：有接入、有配置，但本地保底用内存

### 1. 一句话本质（What）
Redis 在这个项目里的本质，是作为中间件能力预留和运行环境依赖存在，但本地开发阶段为了稳定启动，Sa-Token 实际用了内存 DAO 保底。

### 2. 为什么用它（Why）
项目技术栈里需要 Redis，原因主要有两个：

- 会话和缓存能力的扩展空间
- 与 WebSocket、分布式会话、热点数据缓存的兼容基础

但答辩和本地演示最怕“Redis 没起来项目就挂”，所以我做了本地保底。

### 3. 底层工作原理（How）
1. `pom.xml` 中引入了 `spring-boot-starter-data-redis` 和 `sa-token-redis-jackson`。  
2. `application.yml` 中配置了 `spring.data.redis.host=127.0.0.1`、`port=6379`。  
3. 但 `SaTokenDaoConfig.java` 里显式注入了 `SaTokenDaoDefaultImpl`，也就是**本地开发优先用内存存储**。  
4. 这样做的结果是：Redis 可以启动并作为环境依赖存在，但即使 Redis 暂时不可用，本地基本登录闭环也不至于直接阻断。

### 4. 大厂面试连环拷问（Q&A）
**Q1：既然用了内存 DAO，那 Redis 还有什么意义？**  
A：当前主要是“架构预留 + 环境兼容”。如果要上多实例、统一会话、跨节点状态共享，就需要切回 Redis 存储。现在为了答辩和本地稳定，我优先保证可启动性。

**Q2：面试官问你“你项目真的用了 Redis 吗”，你怎么答？**  
A：我会实话实说：依赖和配置已经接入，当前本地默认采用内存 DAO 做保底启动，Redis 主要作为环境中间件存在；如果进入生产或集群场景，应切换回 Redis 存储以支持分布式会话。

---

# 模块三：前端交互、UI/UX 与网络请求模块

## 20. Axios 封装的本质是“把网络请求统一成一个行为层”

### 1. 一句话本质（What）
Axios 封装不是为了少写几行代码，而是为了统一注入 Token、统一处理错误、统一拆包 `Result`。

### 2. 为什么用它（Why）
这个项目前端接口很多，角色也多。如果每个页面自己手写 `axios.get(...)`，很快就会出现：

- 有的带 Token，有的不带
- 有的处理 401，有的不处理
- 有的拆 `data.data`，有的拆错

所以必须收口。

### 3. 底层工作原理（How）
1. `frontend/src/api/http.ts` 创建单例 Axios 实例。  
2. 请求拦截器里从 `core/session.ts` 读取 `jqpro.token`，注入到 `Authorization` 头。  
3. 响应拦截器先判断是否是统一 `Result` 结构，再按 `code` 解包。  
4. 对 401、403、600、5xx 做集中跳转和提示，页面层只关注业务数据。

### 4. 大厂面试连环拷问（Q&A）
**Q1：为什么前端要认 `SUCCESS_CODES = {0, 200}`？**  
A：因为有些接口约定会用 200 作为业务成功码，有些系统可能习惯用 0。我在封装层兼容两种写法，降低联调成本。

**Q2：如果后端突然返回了非 Result 结构怎么办？**  
A：封装里做了兜底，会把原始响应直接返回，避免整个前端崩掉。但长期看还是应该推动后端保持协议一致。

---

## 21. Token 注入 + 本地会话缓存 + 401 失效处理

### 1. 一句话本质（What）
前端会话管理的本质，是保证“令牌、当前用户、角色首页、登出清理”这几件事在全局上是一致的。

### 2. 为什么用它（Why）
这是一个三角色系统，登录之后要立刻知道：

- 当前是谁
- Token 是什么
- 该跳到学生、老师还是管理员首页
- Token 失效时怎么优雅清退

### 3. 底层工作原理（How）
1. `frontend/src/core/session.ts` 使用 `localStorage` 存 `jqpro.token` 和 `jqpro.current-user`。  
2. 登录成功后通过 `hydrateFromLogin(...)` 同时写入 Token 和用户信息。  
3. `resolveRoleHome(...)` 根据角色返回 `/student`、`/counselor`、`/admin`。  
4. 一旦 `http.ts` 捕获到 401，会清空本地会话并跳回 `/login`。

### 4. 大厂面试连环拷问（Q&A）
**Q1：为什么把 Token 放 localStorage，而不是 Cookie？**  
A：这是典型的前后端分离简化方案，实现成本低。但如果更重视 XSS 防护，我会更倾向于 HttpOnly Cookie + CSRF 策略。

**Q2：本地会话同步怎么做？**  
A：`session.ts` 里会派发 `jqpro:session-changed` 事件，方便各处在会话变化后做同步处理。

---

## 22. 管理端 AI 运维页面为什么最终走“表格直连 CRUD”

### 1. 一句话本质（What）
前端直连 CRUD 的本质，是把高风险操作从“AI 黑盒决定”改回“管理员可见、可改、可确认”的确定性交互。

### 2. 为什么用它（Why）
后台运维在真实使用中发现，复杂 Agent 会带来两个问题：

- 查询结果不稳定
- 删除、修改这种危险动作如果还要依赖二次自然语言理解，出错代价太大

所以我把页面重构成“自然语言只是入口，真正执行仍然是表格 + 弹窗 + 复选框”。

这一块也有明显的前后版本对比：

- **之前怎么做**：自然语言解析之后，前端还试图承接多轮追问、继续补参、再二次走 AI 解释，页面负担很重，状态也容易乱。  
- **现在怎么优化**：前端只做三件事，第一是提交单次指令，第二是展示解析结果，第三是直连编辑/删除/确认接口。AI 留在入口层，真正的高风险操作回到传统后台交互。

### 3. 底层工作原理（How）
1. `AdminAiTaskRoute.vue` 调用 `parseAdminAiTaskApi(...)` 获取解析结果。  
2. 如果是查询结果，就渲染成可编辑、可删除的数据表。  
3. 如果后端返回缺参提示，前端保留原输入框内容，不自动清空，管理员可以直接补完整句子再次发送。  
4. 点击“编辑”会弹出传统表单，再直接调用 `updateAdminUserApi(...)`。  
5. 点击“删除”或批量删除，先勾选再确认，最终直接调用 `deleteAdminUsersApi(...)`。

### 4. 大厂面试连环拷问（Q&A）
**Q1：这不是把 AI 弱化了吗？**  
A：我认为这是正确的降维。AI 最适合做“入口解析”，不适合做“最终执行者”。企业后台的关键不是看起来聪明，而是操作正确、可审计、可撤销。

**Q2：为什么不用原生 table？**  
A：从视觉设计约束上，本项目更强调沉浸式、卡片化和编辑感表达；从交互上看，复合内容、状态标签和行内操作也更适合 Flex/Grid 组织。

---

## 23. 勾选安全比“能点执行”更重要

### 1. 一句话本质（What）
勾选安全的本质，是保证用户看到什么、选中了什么、系统就只执行什么，绝不能偷偷放大操作范围。

### 2. 为什么用它（Why）
这个项目真实踩过一个坑：删除预览里把勾选取消掉后，系统仍然执行了删除。这个问题本质上不是 UI 小瑕疵，而是权限和数据安全问题。

### 3. 底层工作原理（How）
1. `AdminAiTaskRoute.vue` 用 `selectedItemIds` 管理当前勾选的任务项。  
2. 真正执行前，前端只把当前勾选项 ID 发给后端。  
3. 如果一个都没勾选，会直接提示“请至少勾选一项后再执行”，而不是默认全选。  
4. 这保证了预览和执行之间的语义一致。

### 4. 大厂面试连环拷问（Q&A）
**Q1：为什么这种勾选问题值得拿出来讲？**  
A：因为它体现的是“高风险操作默认最小授权”的思想。对面试官来说，这比单纯说会写 Vue 更能体现工程责任心。

**Q2：前端做了校验，后端还要做吗？**  
A：必须做。前端只能提升体验，不能作为安全边界。真正执行前，后端仍要按请求里的 ID 列表严格落库。

---

## 24. 解除 iframe 和嵌套滚动条，本质上是在统一滚动语义

### 1. 一句话本质（What）
“盒中盒”和双滚动条问题，本质上不是 CSS 小问题，而是页面到底由谁负责滚动的语义冲突。

### 2. 为什么用它（Why）
学生资源详情页既有图文文章，又有图片、音频、视频。如果所有内容都塞进固定高度卡片，用户阅读长图文时就会出现：

- 页面外层能滚
- 内部内容区域也能滚
- 体验非常割裂

### 3. 底层工作原理（How）
1. 在 `StudentResourceDetailRoute.vue` 中，用 `previewMode` 区分 article、image、video、audio。  
2. 对图文内容优先用 `v-html` 渲染真实 HTML，只有第三方地址才兜底用 `iframe`。  
3. 对 `iframe` 场景关闭内部滚动并动态调整高度，避免再套一个独立滚动容器。  
4. 对 image 场景移除多余灰底、阴影和固定高度，让内容依赖浏览器唯一原生滚动条。

### 4. 大厂面试连环拷问（Q&A）
**Q1：为什么不一律用 iframe，最省事？**  
A：iframe 省开发，但不省体验。它天生容易造成高度不可控、内部滚动、样式割裂，而且对文章排版的沉浸感也差。

**Q2：`v-html` 会不会有 XSS 风险？**  
A：会，所以它只能用于可信内容源。这个项目里的图文内容来自后台资源库，如果进入更严格生产环境，我会增加 HTML 白名单清洗。

---

## 25. “固定侧边栏 + 右侧局部滚动”是大型后台的可用性基础

### 1. 一句话本质（What）
局部滚动布局的本质，是让导航壳保持稳定，只让真正变化的内容区域滚动。

### 2. 为什么用它（Why）
如果整页一起滚，左侧导航会被带着跑，尤其在长列表和复杂详情页里非常影响定位。这个项目多角色工作台都需要稳定侧边栏。

### 3. 底层工作原理（How）
1. 布局层采用 `height: 100vh; display: flex; overflow: hidden;`。  
2. 左侧导航固定宽度，不允许收缩；右侧主内容区 `flex: 1; overflow-y: auto;`。  
3. `RoleWorkspaceLayout.vue` 里还额外做了侧边栏 `scrollTop` 记忆和恢复。  
4. 路由切换时，导航壳不卸载，避免滚动条回到顶部。

### 4. 大厂面试连环拷问（Q&A）
**Q1：为什么这不是纯视觉问题？**  
A：因为它直接影响任务切换效率。后台系统里导航壳一旦抖动或滚动重置，管理员在高频操作时会非常痛苦。

**Q2：滚动状态持久化放前端做有没有意义？**  
A：有。尤其多级菜单多、内容页长的时候，恢复侧边栏滚动位置能明显降低上下寻找菜单的成本。

---

# 模块四：业务闭环与架构思维

## 26. 三角色系统的本质不是“多几个页面”，而是多条权限闭环

### 1. 一句话本质（What）
学生、咨询师、管理员三角色并不是简单的三套路由，而是三种完全不同的数据边界和业务责任。

### 2. 为什么用它（Why）
这个项目解决的不是单一聊天问题，而是高校心理服务链路问题，所以角色必须清晰分层：

- 学生：自助使用者
- 咨询师：服务提供者
- 管理员：平台治理者

### 3. 底层工作原理（How）
1. 登录成功后，前端根据角色跳转不同首页。  
2. 后端通过 `SaCheckRole` 和业务层关系校验共同控制可见范围。  
3. 学生端主打自助对话、测评、资源、预约；老师端主打档案、报告、真人咨询；管理员主打用户和资源治理。  
4. 三条链路最终都回收到同一套账号体系和审计体系下。

### 4. 大厂面试连环拷问（Q&A）
**Q1：为什么不做成一个角色切换的大界面？**  
A：从权限安全、导航复杂度和认知负担看，三角色分开更清晰。尤其后台管理和学生自助的心智模型完全不同，强行混在一起会让系统又乱又难控。

**Q2：这套角色设计能扩展吗？**  
A：可以。只要继续沿用“角色 + 关系 + 路由壳 + 数据边界”的设计原则，就能继续加学院管理员、超级管理员等角色。

---

## 27. 学生业务闭环：AI 倾诉 → 测评报告 → 资源推荐 → 预约咨询 → 真人聊天室

### 1. 一句话本质（What）
这条闭环的本质，是把学生从“低门槛自助”逐步引导到“真人服务”，而不是一上来就把人推给咨询室。

### 2. 为什么用它（Why）
高校心理支持有一个现实痛点：很多学生不愿直接去约老师，但又确实需要帮助。所以系统要提供一个渐进式路径，而不是单点工具。

### 3. 底层工作原理（How）
1. 学生先通过 AI 聊天或心理测评进行低压力入口体验。  
2. 测评后生成报告和 AI 解读，并结合资源库做推荐。  
3. 当学生需要更深入帮助时，可以进入预约流程选择咨询师和时段。  
4. 预约成功后，进一步过渡到真人聊天室，形成从自助到人工的完整链路。

### 4. 大厂面试连环拷问（Q&A）
**Q1：为什么要做这么长的闭环，而不是只做 AI 聊天？**  
A：因为心理服务不是“聊一次就结束”。真正有价值的是把评估、推荐、预约、干预串起来，这样系统才不是玩具，而是服务平台。

**Q2：这个闭环里最难的是哪一段？**  
A：最难的是 AI 与真人服务之间的衔接，也就是怎么把“情绪表达”过渡成“可管理、可预约、可追踪”的业务流。

---

## 28. 管理端 AI 运维从“炫技 Agent”回归“可控后台”的重构思路

### 1. 一句话本质（What）
这次重构最大的架构价值，不是让 AI 更强，而是明确了“什么该交给 AI，什么必须交给传统后台”。

### 2. 为什么用它（Why）
项目早期尝试过复杂 Agent、多轮上下文、自由工具调用，但后台真实需要的是：

- 查询要稳
- 删除要准
- 修改要可确认
- 缺参数时要能补

所以最后我主动放弃了一部分“看起来很智能”的设计。

这部分我在答辩里会明确讲成“前后两版架构对比”：

- **旧版思路**：多轮上下文 Agent + 大量本地规则兜底。好处是形式上更像智能体，坏处是模型输出、本地规则、前端状态三套东西互相覆盖，最后很难稳定。  
- **新版思路**：AI 主导解析，后端轻校验，前端直连执行。模型负责识别操作类型、挑选 `query_users / create_user / update_users / delete_users`、填扁平参数；后端只做必要的执行前校验、预览和事务落库。

### 3. 底层工作原理（How）
1. 把管理员自然语言输入收敛为“单次无状态解析”。  
2. 把 AI 职责收缩到意图识别、工具选择和扁平参数填充。  
3. 对明显写操作但误调查询工具的情况，只做一次“纠偏提示”，要求模型重新选择正确工具。  
4. 把危险动作重新放回表格预览、复选框勾选、传统表单修改。  
5. 最终执行统一走标准 CRUD 接口，而不是继续做二次 AI 解析。

### 4. 大厂面试连环拷问（Q&A）
**Q1：面试官如果说你这个 AI 做得不够激进，你怎么答？**  
A：我会说，这是一个典型的工程化权衡。能在答辩上稳定跑通、能对错误负责、能防止误删，远比做一个偶尔很聪明、偶尔非常危险的 Agent 更有价值。

**Q2：这算不算“做不成就退回传统方案”？**  
A：不是被动退回，而是主动分层。AI 不是目的，业务稳定才是目的。把 AI 放在最合适的位置，本身就是架构能力。

---

## 29. 高风险操作必须“先预览、再确认、后执行”

### 1. 一句话本质（What）
这是后台治理系统的基本安全原则：**任何可能破坏数据的操作，都不能一步到位。**

### 2. 为什么用它（Why）
像批量删除、批量修改这种操作，如果只靠一句自然语言直接落库，一旦理解错了，就是灾难性后果。

### 3. 底层工作原理（How）
1. 后端先根据条件查出即将受影响的用户列表。  
2. 把这些数据包装成 `AdminAiTaskItem` 返回前端做预览。  
3. 前端展示复选框、修改前后值、影响条数。  
4. 管理员确认后，才真正调用删除或更新接口。

### 4. 大厂面试连环拷问（Q&A）
**Q1：为什么还要复选框，不是已经查出来了吗？**  
A：因为查询条件和执行意图之间可能仍有误差。复选框给管理员最后一次人工裁剪权，这是典型的人机协同设计。

**Q2：如果要支持撤销怎么办？**  
A：短期可以通过审计日志和软删除策略支撑；长期可以做操作快照和补偿事务，但复杂度会明显上升。

---

## 30. 项目环境稳定性本身就是答辩能力的一部分

### 1. 一句话本质（What）
启动稳定性不是杂活，它本质上是工程交付能力。

### 2. 为什么用它（Why）
一个项目如果代码写得再多，但答辩当天 IDEA 一点就报环境错，前端命令起不来，那技术说服力会瞬间下降。

### 3. 底层工作原理（How）
1. 后端统一固定在 Java 17 + Spring Boot 3.2.12。  
2. MySQL、Redis、本地 AI 密钥、IDEA 运行配置都做了启动路径收口。  
3. README 明确写了 MySQL、Redis、后端绿色三角、前端 `npm run dev` 的启动步骤。  
4. 本地开发还通过 `SaTokenDaoConfig.java` 用内存 DAO 降低 Redis 未就绪时的阻断概率。

### 4. 大厂面试连环拷问（Q&A）
**Q1：为什么环境问题值得放进技术总结？**  
A：因为企业研发不是只写代码，更要交付稳定运行的系统。环境稳定、依赖收口、启动可复现，本身就是工程成熟度指标。

**Q2：如果面试官质疑你把密钥写死不规范怎么办？**  
A：我会主动说明：这是为了答辩和本地运行稳定做的演示型保底，生产环境必须改为环境变量或密钥管理服务。这不是忽略安全，而是公开承认不同阶段的工程折中。

---

## 31. “先跑通闭环，再逐步强化安全与体验”是这个项目最核心的架构方法论

### 1. 一句话本质（What）
项目架构不是一开始就追求最复杂，而是先建立可靠闭环，再逐层补安全、补体验、补智能。

### 2. 为什么用它（Why）
这个项目从学生聊天、老师查看、管理员治理，到 AI 运维，功能链很多。如果一开始每个点都追求最理想解，项目很容易变成“每个模块都半成品”。

### 3. 底层工作原理（How）
1. 第一阶段先保证账号、角色、预约、聊天、资源、报告这些基本链路能跑。  
2. 第二阶段补充安全：密码加盐哈希、消息加密、权限边界、审计日志。  
3. 第三阶段再引入大模型，并在后台运维中反复做降维和保底。  
4. 第四阶段聚焦 UI/UX、滚动体验、状态同步和启动稳定性。

### 4. 大厂面试连环拷问（Q&A）
**Q1：你这个方法论和“敏捷开发”有什么关系？**  
A：本质很接近。都是先跑 MVP 闭环，再按风险和收益排序逐步强化。但我这里更强调“安全和稳定性优先级”，这和心理健康场景有关。

**Q2：如果让你重做一次，你会怎么调整顺序？**  
A：我会更早把后台 AI 运维限定成“单次解析 + 直连 CRUD”，避免在复杂 Agent 上花太多时间。这个是我真实项目复盘后的结论。

---

## 32. 你这个项目最值得在面试里强调的，不是“用了多少技术”，而是“做对了多少工程判断”

### 1. 一句话本质（What）
高级工程能力的核心，不是会不会堆技术，而是知道什么时候该激进、什么时候该保守、什么时候该降维。

### 2. 为什么用它（Why）
面试官真正想看的是：

- 你是否理解技术和业务之间的边界
- 你是否知道安全、可控、稳定为什么重要
- 你是否能在项目出问题时做出正确的架构收敛

这个项目最有价值的地方，恰恰就在这些判断。

### 3. 底层工作原理（How）
1. 在 AI 侧，我没有迷信“越智能越好”，而是把模型收缩为可控解析器。  
2. 在安全侧，我不仅做了账号口令保护，还对敏感聊天内容做了落库加密。  
3. 在权限侧，我没有停留在 RBAC，而是做了角色 + 数据关系联合判定。  
4. 在前端侧，我没有把精力全花在漂亮效果上，而是优先解决状态同步、勾选安全和滚动语义。

### 4. 大厂面试连环拷问（Q&A）
**Q1：你觉得这个项目里最像“大厂思维”的地方是什么？**  
A：不是某个框架，而是我对高风险链路做了降维保底，对敏感数据做了安全兜底，对复杂权限做了关系校验，对环境启动做了稳定性收口。这些都不是炫技，而是工程责任。

**Q2：如果只允许你从这个项目带走 3 个能力去面试，你带什么？**  
A：第一是 AI 工程化里的“模型辅助、后端兜底”思维；第二是安全方向的“鉴权、加密、审计、关系权限”组合拳；第三是复杂项目里“能复盘、能降维、能收口”的架构判断力。

---

# 最后一页：答辩与面试时的输出策略

## 1. 如果老师问：“你这个项目最难的点是什么？”

你优先答这三层：

1. **不是某个单一 bug，而是多模块闭环协同**
2. **尤其是 AI 能力怎么和真实后台治理、安全边界对齐**
3. **最终我通过降维、分层、预览确认和统一接口把它收口了**

---

## 2. 如果老师问：“你这个项目的安全性做了什么？”

你可以按这个顺序答：

1. 账号密码不是明文，做了**随机盐 + SHA3-256 哈希**
2. 聊天内容不是明文落库，做了**AES 加密存储**
3. 接口不是只看登录，还做了**Sa-Token 鉴权 + 角色校验**
4. 敏感数据不是只看角色，还做了**老师与学生的关系权限判断**
5. 关键操作都有**审计日志**

---

## 3. 如果老师问：“AI 为什么没有全部交给模型处理？”

你可以直接说：

> 因为我把它当成后台治理系统，而不是玩具 Demo。  
> 简单 CRUD 场景里，模型最适合做的是自然语言理解，而不是最终执行。  
> 真正执行数据库修改的责任必须留给后端接口、权限系统、事务和审计日志。  
> 这不是妥协，而是工程化的安全边界设计。

---

## 4. 如果面试官问：“这个项目你最大的成长是什么？”

推荐答法：

> 我最大的成长不是学会了多少框架，而是第一次真实体会到：  
> 架构不是把系统设计得越复杂越好，而是知道什么时候该加能力，什么时候该减能力。  
> 这个项目里我对 AI、多角色权限、敏感数据保护和前后端状态同步都做了取舍，最后把系统收敛成一个能稳定演示、能自圆其说、也能经得住追问的版本。  
> 我觉得这比单纯把功能堆出来更像工程能力。

---

# 结语

如果你把这份文档真正吃透，你在答辩和面试里要传达的就不再是：

- “我会 Vue”
- “我会 Spring Boot”
- “我接过大模型”

而是：

- **我知道怎么把一个 AI 项目做成真正可运行、可治理、可防守的系统**
- **我理解账号安全、权限边界、敏感数据保护和日志追责的底层逻辑**
- **我不是只会写功能，我也会做工程判断**

这才是这类项目真正能转化成求职竞争力的地方。
